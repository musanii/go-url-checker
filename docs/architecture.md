
### 4. `docs/architecture.md`

```markdown
# Architecture

## Current Architecture

The project currently contains a small URL-checking domain package.

```text
internal/
└── checker/
    │
    ├── CheckURL
    │
    └── CheckURLWithTimeout